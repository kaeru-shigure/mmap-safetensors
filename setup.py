from setuptools import setup, find_packages

setup(
    name='mmap-safetensors',
    version="0.0.1",
    description='memory-efficient safetensors reader using mmap',
    author='kaeru-shigure',
    packages=find_packages(),
    license='MIT'
)
